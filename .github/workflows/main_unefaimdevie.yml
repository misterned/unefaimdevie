# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions
# More info on Python, GitHub Actions, and Azure App Service: https://aka.ms/python-webapps-actions

name: Deploy Azure App Service
run-name: Deploy Azure (${{ github.event_name == 'workflow_dispatch' && github.event.inputs.deploy_ref || github.ref_name }}) @ ${{ github.sha }}

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      deploy_ref:
        description: "Branch or tag to deploy"
        required: true
        default: "main"

concurrency:
  group: azure-deploy
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read #This is required for actions/checkout

    steps:
      - uses: actions/checkout@v4.2.2
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.deploy_ref || github.ref_name }}

      - name: Show revision to deploy
        run: |
          echo "event=${{ github.event_name }}"
          echo "selected_ref=${{ github.event_name == 'workflow_dispatch' && github.event.inputs.deploy_ref || github.ref_name }}"
          echo "branch_from_git=$(git rev-parse --abbrev-ref HEAD)"
          echo "commit=$(git rev-parse HEAD)"

      - name: Set up Python version
        uses: actions/setup-python@v5.6.0
        with:
          python-version: '3.x'

      # 🛠️ Local Build Section (Optional)
      # The following section in your workflow is designed to catch build issues early on the client side, before deployment. This can be helpful for debugging and validation. However, if this step significantly increases deployment time and early detection is not critical for your workflow, you may remove this section to streamline the deployment process.
      - name: Create and Start virtual environment and Install dependencies
        run: |
          python -m venv antenv
          source antenv/bin/activate
          pip install -r requirements.txt
                
      # By default, when you enable GitHub CI/CD integration through the Azure portal, the platform automatically sets the SCM_DO_BUILD_DURING_DEPLOYMENT application setting to true. This triggers the use of Oryx, a build engine that handles application compilation and dependency installation (e.g., pip install) directly on the platform during deployment. Hence, we exclude the antenv virtual environment directory from the deployment artifact to reduce the payload size. 
      - name: Upload artifact for deployment jobs
        uses: actions/upload-artifact@v4.6.2
        with:
          name: python-app
          path: |
            .
            !antenv/

      # 🚫 Opting Out of Oryx Build
      # If you prefer to disable the Oryx build process during deployment, follow these steps:
      # 1. Remove the SCM_DO_BUILD_DURING_DEPLOYMENT app setting from your Azure App Service Environment variables.
      # 2. Refer to sample workflows for alternative deployment strategies: https://github.com/Azure/actions-workflow-samples/tree/master/AppService
      

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: read

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4.3.0
        with:
          name: python-app

      - name: Verify artifact content
        run: |
          echo "artifact_commit_check=$(grep -nF 'Que trouverez vous ici ?' core/templates/home.html || true)"
          echo "artifact_home_preview:"
          sed -n '1,40p' core/templates/home.html

      - name: Create deploy marker
        run: |
          printf "%s\n" "${GITHUB_SHA}" > .deploy-sha
          printf "%s\n" "$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> .deploy-sha
          echo "deploy_marker:"
          cat .deploy-sha

      - name: 'Deploy to Azure Web App'
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'unefaimdevie'
          slot-name: 'Production'
          package: .
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}

      - name: Validate deployment target
        run: |
          set -euo pipefail
          DEPLOYED_URL="${{ steps.deploy-to-webapp.outputs.webapp-url }}"
          echo "deployed_webapp_url=$DEPLOYED_URL"
          if [ -n "$DEPLOYED_URL" ] && [[ "$DEPLOYED_URL" != *"unefaimdevie.azurewebsites.net"* ]]; then
            echo "Warning: deployment target mismatch. Check AZURE_WEBAPP_PUBLISH_PROFILE secret."
            exit 0
          fi

      - name: Verify live homepage content (best effort)
        run: |
          set -euo pipefail
          LIVE_URL="${{ steps.deploy-to-webapp.outputs.webapp-url }}"
          if [ -z "$LIVE_URL" ]; then
            LIVE_URL="https://unefaimdevie.azurewebsites.net/"
          fi
          LIVE_URL="${LIVE_URL%/}/"
          echo "live_url=$LIVE_URL"

          fetched=0
          for attempt in 1 2 3; do
            if curl -fsSL "$LIVE_URL" -o live_home.html; then
              fetched=1
              break
            fi
            echo "Live check attempt $attempt failed"
          done

          if [ "$fetched" -eq 0 ]; then
            echo "Warning: live homepage check skipped (network/DNS issue from runner)."
            exit 0
          fi

          echo "live_home_match=$(grep -nF 'Que trouverez vous ici ?' live_home.html || true)"
          if ! grep -qF 'Que trouverez vous ici ?' live_home.html; then
            echo "Warning: live site does not contain expected text after deployment"
            exit 0
          fi
          